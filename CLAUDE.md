# CLAUDE.md — claude-code-docs 규칙

Claude Code 문서를 전 페이지 통과하며 내 것으로 만드는 레포. 아래 규칙을 따른다.

---

## 노트 작성 규칙

- 문서 내용을 요약해서 복붙하지 않는다. 원문이 항상 더 정확하고 최신이다.
- 노트에는 **예상과 실제의 차이**, **기능 간 경계**, **손으로 확인한 결과**, **남은 질문**만 적는다.
- 노트 형식은 `notes/TEMPLATE.md`를 따른다.

## COVERAGE.md 갱신 규칙

- 페이지를 다 읽으면 `COVERAGE.md`의 해당 행 상태와 확인 버전/날짜를 갱신한다.
- 상태는 4단계: ⬜ → 📖 → 🔧 → 🧠. ⛔ 는 명시적으로 판단한 경우에만.
- ⛔ 로 표기할 때는 반드시 이유를 한 줄 적는다.
- 주 1회 whats-new 최신 주차 페이지를 확인하고 `log/`에 3줄 남긴다. 새 페이지가 생겼으면 `COVERAGE.md`에 행을 추가한다.

## 문서 참조 규칙

- 기능을 언급할 때는 항상 공식 문서 URL을 함께 남긴다.
  - 문서 인덱스: https://code.claude.com/docs/en/claude_code_docs_map.md
- 문서에 실제로 있는지 확인하지 않은 기능명·플래그·버전은 추측해서 쓰지 않는다. 모르면 모른다고 적는다.

## 실험 규칙

- 실험은 `labs/NNN-주제/`에 만들고 README에 **질문 / 실험 / 결론** 3섹션만 쓴다.
- lab은 버려도 되는 것, `notes/`와 `COVERAGE.md`는 남는 것으로 취급한다.
- labs 안에 예시 프로젝트를 미리 만들어두지 않는다.

## 역색인 규칙

- 노트를 쓰다가 "이런 요구사항이면 이걸 쓰겠구나" 싶을 때 `index/by-need.md`에 한 줄 적립한다.
- `index/by-need.md`는 노트에서 파생되어야 한다. 미리 채우지 않는다.
